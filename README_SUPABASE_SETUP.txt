STUDYBEAR — SUPABASE AUTH V30

1. Supabase -> SQL Editor: run supabase_auth_bootstrap.sql once.
2. Verify Email provider is Enabled and Confirm email is ON if you want email verification.
3. GitHub Pages: upload index.html, styles.css, script.js, supabase.js.
4. supabase.js already contains your Project URL and Publishable Key. Never replace it with a secret/service_role key.
5. Existing LocalStorage accounts remain available as a temporary migration fallback. New accounts use Supabase Auth.
6. New signup requires a real email address because email confirmation is enabled.
